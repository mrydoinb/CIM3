{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: 当前文件 (CIM Road POC)",
            "type": "python",
            "request": "launch",
            "program": "${file}",
            "console": "integratedTerminal",
            "cwd": "${workspaceFolder}",
            "justMyCode": true,
            "showReturnValue": true
        }
    ]
}